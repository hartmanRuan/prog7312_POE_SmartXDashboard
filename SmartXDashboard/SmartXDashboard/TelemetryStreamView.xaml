<UserControl x:Class="SmartXDashboard.TelemetryStreamView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="550" d:DesignWidth="800">

    <UserControl.Resources>
        <!-- Dark Input Field Style -->
        <Style x:Key="ModernTextBoxStyle" TargetType="TextBox">
            <Setter Property="Background" Value="#18181C"/>
            <Setter Property="Foreground" Value="#FFFFFF"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Padding" Value="8,8,8,8"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#2A2A32"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="border" Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                CornerRadius="6">
                            <ScrollViewer x:Name="PART_ContentHost" Margin="0"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="#FFC700"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Secondary Button Style -->
        <Style x:Key="YellowActionButton" TargetType="Button">
            <Setter Property="Background" Value="#18181C"/>
            <Setter Property="Foreground" Value="#FFC700"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Padding" Value="12,8,12,8"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="6" 
                                BorderBrush="#FFC700" BorderThickness="1" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#FFC700"/>
                                <Setter Property="Foreground" Value="#0E0E10"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- DataGrid Dark Theme Styling -->
        <Style TargetType="DataGrid">
            <Setter Property="Background" Value="#141418"/>
            <Setter Property="RowBackground" Value="#18181C"/>
            <Setter Property="AlternatingRowBackground" Value="#141418"/>
            <Setter Property="Foreground" Value="#FFFFFF"/>
            <Setter Property="BorderBrush" Value="#2A2A32"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="GridLinesVisibility" Value="Horizontal"/>
            <Setter Property="HorizontalGridLinesBrush" Value="#2A2A32"/>
            <Setter Property="HeadersVisibility" Value="Column"/>
            <Setter Property="CanUserAddRows" Value="False"/>
            <Setter Property="AutoGenerateColumns" Value="False"/>
            <Setter Property="FontSize" Value="12"/>
        </Style>

        <Style TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="#1C1C22"/>
            <Setter Property="Foreground" Value="#FFC700"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Padding" Value="10,8,10,8"/>
            <Setter Property="BorderBrush" Value="#2A2A32"/>
            <Setter Property="BorderThickness" Value="0,0,1,1"/>
        </Style>

        <Style TargetType="DataGridCell">
            <Setter Property="Padding" Value="10,8,10,8"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="DataGridCell">
                        <Border Padding="{TemplateBinding Padding}" Background="{TemplateBinding Background}">
                            <ContentPresenter VerticalAlignment="Center" />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="#1E1E24"/>
                    <Setter Property="Foreground" Value="#FFC700"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <!-- Top Controls Header -->
            <RowDefinition Height="Auto"/>
            <!-- Data Grid Stream -->
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- TOP CONTROLS BAR -->
        <Grid Grid.Row="0" Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <!-- Search & Filter Inputs -->
            <StackPanel Grid.Column="0" Orientation="Horizontal">
                <StackPanel Width="220" Margin="0,0,12,0">
                    <TextBlock Text="FILTER BY MAC / ID" FontSize="10" Foreground="#8A8A93" FontWeight="Bold" Margin="0,0,0,4"/>
                    <TextBox x:Name="SearchMacInput" Style="{StaticResource ModernTextBoxStyle}" TextChanged="SearchMacInput_TextChanged"/>
                </StackPanel>

                <StackPanel Width="180">
                    <TextBlock Text="STATUS SEVERITY" FontSize="10" Foreground="#8A8A93" FontWeight="Bold" Margin="0,0,0,4"/>
                    <ComboBox x:Name="StatusFilterComboBox" Height="32" Background="#18181C" Foreground="#FFFFFF" SelectionChanged="StatusFilterComboBox_SelectionChanged">
                        <ComboBoxItem Content="All Streams" IsSelected="True"/>
                        <ComboBoxItem Content="Normal"/>
                        <ComboBoxItem Content="Warning"/>
                        <ComboBoxItem Content="Critical"/>
                    </ComboBox>
                </StackPanel>
            </StackPanel>

            <!-- Simulation Action -->
            <StackPanel Grid.Column="1" VerticalAlignment="Bottom">
                <Button Content="⚡ SIMULATE TELEMETRY PACKET" Style="{StaticResource YellowActionButton}" Click="SimulatePacket_Click"/>
            </StackPanel>
        </Grid>

        <!-- TELEMETRY STREAM DATA TABLE -->
        <DataGrid x:Name="TelemetryGrid" Grid.Row="1">
            <DataGrid.Columns>
                <DataGridTextColumn Header="TIMESTAMP" Binding="{Binding Timestamp}" Width="150"/>
                <DataGridTextColumn Header="NODE MAC ID" Binding="{Binding MacAddress}" Width="140"/>
                <DataGridTextColumn Header="ZONE" Binding="{Binding Zone}" Width="120"/>
                <DataGridTextColumn Header="READING VALUE" Binding="{Binding PayloadValue}" Width="120"/>
                <DataGridTextColumn Header="METRIC" Binding="{Binding MetricType}" Width="120"/>
                <DataGridTextColumn Header="STATUS" Binding="{Binding Status}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>